# GitHub これだけ覚えるシート（Swift 1年生版）

> Swift（1年生・後期）第2回配布資料。授業中いつでも見返してください。

---

## ■ やることは3つだけ

この授業で GitHub を使ってやることは、たった3つです。

1. **学習ノート（NOTE.md）を編集して保存する（コミット）**
2. **演習を終えた main.swift をアップロードして保存する（Upload files）**
3. **教材コードをコピーして Xcode に貼る（Raw）**

これ以外の GitHub の機能（Issue、Pull Request、ブランチ）は、この授業では使いません。

---

## ■ STEP 1：GitHub のアカウントを作る（最初に1回だけ）

すでにアカウントがある人は、そのまま使ってかまいません（学校のメールで作ったものでも、個人のものでも可）。ユーザー名を控えておいてください。

1. https://github.com/signup を開く
2. メールアドレスを入力（学校のメール `26cm01XX@jec.ac.jp` を推奨）
3. パスワードを決める（15文字以上、または 英字と数字を混ぜて8文字以上。記号を1つ入れると確実）
4. ユーザー名を決める。**このユーザー名はずっと残ります。就職活動で見られても困らない名前**にしてください（例：`taro-nakagawa`、`nakagawa-jec`）。英数字とハイフンだけ使えます
5. 確認メールに書いてあるコードを入力して完了

💡 確認メールが来ないときは、迷惑メールフォルダを確認。5分待っても来なければ「Resend verification email」を押す。

---

## ■ STEP 2：学習ノートのテンプレートを Fork する（最初に1回だけ）

「Fork」とは、他の人のリポジトリを自分のアカウントにコピーすることです。コピーした後は自分のリポジトリになるので、自由に編集できます。

1. https://github.com にログイン
2. https://github.com/NakasenJEC/swift-grammar-template を開く
3. 右上の「**Fork**」ボタンをクリック
4. Owner が自分のユーザー名になっていることを確認して「**Create fork**」をクリック → 完了！

これで、学習ノートの全テンプレート（README、festival、week01〜week10）が自分のアカウントにコピーされます。

💡 Fork ボタンが見つからないときは、このアドレスを直接開く: https://github.com/NakasenJEC/swift-grammar-template/fork

💡 Fork した後の自分の学習ノートのアドレスは `https://github.com/自分のユーザー名/swift-grammar-template` です。ブックマークしておきましょう。

---

## ■ STEP 3：ファイルを編集して保存する（NOTE.md、README.md）

1. 自分のリポジトリ（Fork した学習ノート）を開く
2. 編集したいファイル（例：`README.md`）をクリックして開く
3. 右上の **鉛筆アイコン** ✏️ をクリック
4. 内容を修正する
5. **「Preview」タブをクリックして見た目を確認する**（何度でも切り替えてよい）
6. 右上の「**Commit changes...**」→ 出てきた画面で「**Commit changes**」をクリック

💡 **「Commit」＝保存** と思ってかまいません。変更の履歴が自動で残ります。

💡 鉛筆アイコンが無いときは、先生のリポジトリ（NakasenJEC）を開いています。自分の Fork を開き直してください。アドレスに自分のユーザー名が入っているのが目印です。

---

## ■ STEP 4：main.swift をアップロードして上書きする（Upload files）

金曜の演習で、教材コードのチェックが全部 `OK` になったら、その `main.swift` を自分の学習ノートに上書きします。

1. 自分の学習ノートで、その週のフォルダ（例：`week01`）を開く
2. 右上の「**Add file**」→「**Upload files**」
3. Finder から `main.swift` をドラッグ＆ドロップする（Xcode のプロジェクトフォルダの中にあります）
4. 「**Commit changes**」をクリック

同じ名前のファイル（`main.swift`）があるので、上書きされます。フォルダを間違えないでください（`week01` に `week02` の main.swift を上げると、`week01` の課題が消えます）。

💡 Xcode のプロジェクトフォルダは、Xcode の左のファイル一覧で `main.swift` を右クリック →「**Show in Finder**」で開けます。

---

## ■ STEP 5：教材コードを Raw からコピーする（毎週）

教材コードは ZIP ではなく、画面からコピーします。

1. https://github.com/NakasenJEC/swift-grammar-code を開く
2. その週のフォルダ（例：`week01`）→ `main.swift` をクリック
3. 右上の「**Raw**」ボタンをクリック（文字だけの画面になります）
4. ⌘A で全部選択 → ⌘C でコピー
5. Xcode で **File > New > Project... > macOS > Command Line Tool** を選び、名前を `week01` にして作る
6. 作られた `main.swift` の中身を全部消して ⌘V で貼る
7. ⌘R で実行 → 下のコンソールに `OK` と `NG` の行が出れば成功

---

## ■ Markdown 記法：これだけ覚える

学習ノートはテンプレートに沿って埋めるだけなので、覚える記法は4つです。

### 見出し

```markdown
## 中見出し
### 小見出し
```

### 太字

```markdown
**このように囲むと太字になる**
```

### Swift のコードを載せる

バッククォート3つで囲みます。バッククォートは `Shift + @`（日本語キーボード）で入力できます。

````markdown
```swift
let scores = [90, 75, 60]
print(scores.count)
```
````

### 表

```markdown
| 項目 | 説明 |
|------|------|
| let | 変えられない |
| var | 変えられる |
```

---

## ■ まとめ：毎週の操作フロー

**月曜（対面）**

```
① 確認テスト（Forms）
    ↓
② 教材コードを Raw からコピーして Xcode で実行（STEP 5）
    ↓
③ NOTE.md の「教材コードで読めたこと」を書いて Commit（STEP 3）
```

**金曜（リモート）**

```
① 演習の NG を OK にする（生成AI 可。4点セットを NOTE.md に書きながら）
    ↓
② NOTE.md を書いて Commit（STEP 3）
    ↓
③ main.swift を Upload files で上書きして Commit（STEP 4）
```

最低ラインは毎回「コミット1回」です。書きかけでかまいません。

---

## ■ 困ったとき

| 症状 | 対処 |
|------|------|
| 確認メールが来ない | 迷惑メールフォルダ → 「Resend verification email」 → メールアドレスの打ち間違いを確認 |
| ユーザー名が取られている | 後ろに `-jec` や数字を付ける |
| Fork ボタンが見えない | ログインしているか確認 → 直接リンク `.../swift-grammar-template/fork` |
| 鉛筆アイコンが無い | 先生のリポジトリを開いている。自分の Fork を開く |
| Commit changes が押せない | 何も変更していないと押せない。何か1文字変える |
| Upload files で上書きされない | ファイル名が `main.swift` か、フォルダが合っているか確認 |
| Raw を貼っても動かない | 貼る前に Xcode の `main.swift` の中身を全部消したか確認。Swift Language Version が 6 になっているか確認 |

分からなければ、先生に声をかけるか、リモートならチャットに「今開いているページの URL」を貼ってください。URL を見ればすぐ分かります。

---

## ■ もっと知りたい人へ

自分の Xcode プロジェクトそのものを GitHub で管理したい人は、別紙「Xcode × GitHub チートシート」を読んでください。希望者だけでかまいません。
