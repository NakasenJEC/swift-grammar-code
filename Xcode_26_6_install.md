# Xcode 26.6 の入れ方（Apple Developer から）

> Swift（1年生・後期）配布資料。この授業は Xcode 26.6 で進めます。
> 期限は 10/26（月・第10回）です。授業時間外に、家か学校の空き時間でやってください。ダウンロードと展開に合わせて 30 分〜1 時間かかります。

---

## ■ まず確認（3分）

1. 画面左上の Apple メニュー →「このMacについて」で macOS のバージョンを見る。**26.2 以上なら OK** です。26.1 以下の人は先生に相談してください（Xcode 26.6 は macOS 26.2 以上が必要です）
2. Xcode を開き、メニューの Xcode → About Xcode でバージョンを見る。**26.6 なら、この資料の作業は要りません**
3. 「このMacについて」→「詳細情報…」で開く画面の「ストレージ」で、空き容量が **30GB 以上**あることを確認する（Xcode 本体で約 10GB、iPhone のシミュレータで約 8GB 使います）

- 学校の MacBook Air は **App Store で更新しません**（他の科目のアプリが使えなくなることがあります）。必ずこの資料の手順で入れてください
- 自分の Mac の人も同じ手順で入れられます。Xcode 27 はこの授業では扱いません。入れるのは自由ですが、27 で起きた問題は授業では見られません

---

## ■ STEP 1：ダウンロードする

1. https://developer.apple.com/download/all/?q=Xcode を開く
2. Apple Account でサインインする（4月に作った、学籍番号のメールアドレスのアカウント）。有料の登録は要りません
3. 一覧から **Xcode 26.6** を探し、**Apple silicon** 用の xip ファイル（`Xcode_26.6_Apple_silicon.xip`、約 2.3GB）をダウンロードする
4. 終わるまで待つ。学校の Wi-Fi でも家の回線でもかまいません

---

## ■ STEP 2：展開して、古い Xcode と置き換える

1. Xcode を使っていたら終了する（Xcode → Quit Xcode）
2. 「ダウンロード」フォルダの `Xcode_26.6_Apple_silicon.xip` をダブルクリックする。「アーカイブユーティリティ」が開いて展開が始まります（5〜10 分）
3. 同じフォルダに `Xcode.app` ができる
4. 「アプリケーション」フォルダにある古い `Xcode.app` をゴミ箱に入れる
5. 新しい `Xcode.app` を「アプリケーション」フォルダに移動する
6. `Xcode_26.6_Apple_silicon.xip` はゴミ箱に入れてよい（2.3GB 空きます）

- 古い Xcode を残したい人は、4 の代わりに古い方の名前を `Xcode-26.5.app` のように変えてから 5 をする。ただし容量を 10GB 多く使うので、学校の Mac ではおすすめしません

---

## ■ STEP 3：最初に起動する

1. 「アプリケーション」フォルダの Xcode を開く
2. 「Xcode and Apple SDKs Agreement」が出たら **Agree**。Mac のパスワード（または Touch ID）を求められたら入力する。これは Apple Account のパスワードではなく、Mac にログインするときのパスワードです
3. インストールするプラットフォームを選ぶ画面が出たら、**iOS** にチェックが入っていることを確認して Install（Download & Install）を押す。日専祭のアプリで iPhone のシミュレータを使うので必要です（約 8GB、10〜20 分）
4. Xcode → About Xcode で **Version 26.6** と出れば完了です

---

## ■ 困ったとき

| 症状 | 対処 |
|------|------|
| ダウンロードの一覧に何も出ない | サインインできているか確認する。検索欄に `Xcode` と入れる |
| 「macOS 26.2 以上が必要」と言われる | macOS が古い。学校の Mac は自分で更新せず、先生に相談する |
| 空き容量が足りない | 「ダウンロード」フォルダの古いファイルや使っていないアプリを消す。シミュレータ（STEP 3 の 3）は後からでも入れられる |
| 展開が終わらない | 電源につないで待つ。10 分以上進まなければ Mac を再起動して、STEP 2 の 2 からやり直す |
| Xcode を開くと「開発元を検証中」で止まる | そのまま待つ。1〜2 分かかることがあります |
| 途中で分からなくなった | 9/28 か 10/26 の対面授業で先生に見せてください。ダウンロードだけ済ませておくと当日が楽です |
